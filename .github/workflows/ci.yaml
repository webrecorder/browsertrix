name: Browsertrix Cloud Integration Test (for Swarm)

on: [push]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - run: test/setup.sh

      - run: curl http://localhost:9871/api/settings

