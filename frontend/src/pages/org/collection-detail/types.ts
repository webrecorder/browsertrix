export enum Tab {
  Replay = "replay",
  About = "about",
  Items = "items",
  Deduplication = "deduplication",
}

export enum CollectionSearchParam {
  Editing = "editing",
}

export enum EditingSearchParamValue {
  Items = "items",
}
