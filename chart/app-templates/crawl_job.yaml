apiVersion: btrix.cloud/v1
kind: CrawlJob
metadata:
  name: {{ "qa-" if is_qa else "" }}crawljob-{{ id }}
  labels:
    crawl: "{{ id }}"
    role: {{ "qa-job" if is_qa else "job" }}
    btrix.org: "{{ oid }}"
    btrix.user: "{{ userid }}"
    btrix.storage: "{{ storage_name }}"

spec:
  selector:
    matchLabels:
      crawl: "{{ id }}"

  id: "{{ id }}"
  userid: "{{ userid }}"
  cid: "{{ cid }}"
  oid: "{{ oid }}"
  scale: {{ scale }}

  maxCrawlSize: {{ max_crawl_size if not is_qa else 0 }}
  timeout: {{ timeout if not is_qa else 0 }}

  manual: {{ manual }}
  crawlerChannel: "{{ crawler_channel }}"
  ttlSecondsAfterFinished: 30
  warcPrefix: "{{ warc_prefix }}"

  storageName: "{{ storage_name }}"

